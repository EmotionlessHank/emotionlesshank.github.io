# Lessons — tag map

> One pitfall = one `L-NNN-<slug>.md` file. Read this map before dev; deep-read an entry only
> when the current task matches its tags. Hit a new pitfall → append an `L-NNN` file + a row here.

| ID | Pitfall | Tags |
|---|---|---|
| _(none yet)_ | | |
